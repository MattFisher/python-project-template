# node-modules rather than the Yarn Berry default of PnP: this fixture exists to
# exercise node-ci's corepack/setup-node wiring, and PnP would add its own
# failure modes (loader flags, SDK shims) that have nothing to do with that.
nodeLinker: node-modules
