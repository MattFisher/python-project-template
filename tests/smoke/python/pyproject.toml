[project]
name = "python-ci-smoke"
version = "0.0.0"
description = "Fixture exercised by python-ci.yml in template CI. Not published."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["mypy>=1.14", "pytest>=8", "pre-commit>=4"]

[tool.uv]
package = false
