[project]
name = "python-ci-smoke"
version = "0.0.0"
description = "Fixture exercised by python-ci.yml in template CI. Not published."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["basedpyright>=1.39", "pytest>=8", "pre-commit>=4"]

[tool.uv]
package = false

[tool.basedpyright]
typeCheckingMode = "strict"
